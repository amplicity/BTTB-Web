== README

Bang to the Beat was a music blog I created, did not have time to keep up with it.  It uses minimal code and plays tracks via SoundCloud API.

Clone and run <tt>rake db:migrate</tt>, <tt>bundle install</tt>, then <tt>rails s</tt> to boot. 

